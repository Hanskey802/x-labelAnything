# 20260706 准备切入
它会在C:\Users\Administrator\位置生成xanylabeling_data文件夹，用它来保存数据。告诉我怎么把这个位置改掉
ai这个按钮下有autolabeling的功能，如何选择我自己的模型去自动标注呢
C:\Users\Administrator\xanylabeling_data 这默认路径改为工程路劲下E:\WorkSpace\ClearML\x-labelAnything中

# 20260708 切分图像
软件左侧面板添加一个按钮，实现功能：按照输入的行列数，切分已打开路径下的所有图像，如果进行切分，此文件夹中所有图被替换成切分后的小图，原图保留到上一级路路径，文件夹命名“原文件夹_raw”；


# 20260709 自动标注
1. 打开文件夹路径，记住上一次打开的位置。
2. 批量导出json标注文件到yolo标注数据时，输入路径默认为当前打开的文件夹路径“pathDir”，输出路径为“pathDir”+“—yolo_dataset”.
3. 自动标注加载custom_models的配置yaml文件，然后找到对应的onnx，我不知道与onnx相应的classes包括那些，请删掉classes后的类别，动态的从onnx中读取类别，并应用类别到自动标注。
4. 自动标注部分，请告诉我它的yolov5的推理部分在哪里？
5. 把历史对话的提示词都整理到这个文档中。表明时间、流程、意义、对应的代码位置。

# 补充 模型配置
1. 需求：在自动AI标注的功能模块中，UI界面中“没有模型”按钮前面需要增加一个“配置模型”按钮，此按钮有包含以下几个功能：
 1.1 点击按钮弹出一个配置界面，需要配置的选项有：type、name、provider、display_name、model_path、iou_threshold、conf_threshold。
 1.2  type：为枚举类型，没有现有的所有的Yolo系列类型。
 1.3  name：“输入值”+“_”+type
 1.4  provider: const固定值“custom”
 1.5  display_name和name相同，可以隐藏输入，只输入name就行。
 1.6  model_path：打开窗口输入openFileDialog，选择onnx文件，过滤器设置为“*.onnx”
 1.7  iou_threshold、conf_threshold：常规浮点输入，值域（0，1）

# 20260710 能不能接入训练系统
1. 我本地局域网中有一台服务器，已经部署了clearml 训练agent，如何通过这台计算机下达训练任务，我有已经转yolo格式的数据集，是否需要把数据集拷贝到服务器上？帮我设计一下.
1. 现在标注数据集已经搞定了，帮我设计一下如何进行训练，以充分利用资源，以下是我所有硬件设备情况：
 - 笔记本A：intel12700K+ 英伟达rtx3060（6G）+内存16G + windows操作系统
 - 服务器B：intel14700K + 英伟达rtx4090D（48G）+英伟达rtx5090（24G）+内存192G+ linux系统
核心需求：

# 【20260713】增加训练功能
1. D:\Work_mumu\LabelAndTrain\x-train\yolov5-7.0这个工程源码不要改动。
2. 使用conda配置适合yolov5-7.0工程的虚拟环境。
3. D:\Work_mumu\LabelAndTrain\x-labelAnything这个项目中，增加“训练”功能。详细如下：

顶部菜单栏添加一个“训练”按钮。点击训练按钮，实现以下功能：
通过clearml去给yolov5创建任务。训练集位置、训练参数、等通过什么形式传入呢？
整体帮我设计一些，核心就是，我点击训练，yolov5开始训练，训练结果放入数据集的“train_result”文件夹中。

# 【20260714】修复训练功能
1. “训练”功能按钮我选择合适的训练集、训练参数，开始训练结果出错了，你debug处理一下。
2. 训练功能全部去除clearml，改为使用本地训练。
## debug问题
  1. 数据集文件格式不对,导致找不到数据集。
  2. 应该是导出yolo数据集的时候，把数据集的格式转换成yolo数据集的时候出错了。
  3. "D:\Img\dataset\case0-yolo_dataset\dataset.yaml"这是我测试的dataset.yaml文件。
## 补：
1. 训练过程的信息打印有乱码，优化乱码，训练过程中要绘制训练过程信息，如map、loss，图形界面美观。
2. 导出yolo数据集的时候，生成一份默认超参数文件，训练的时候读取这个文件，并使用这个超参数文件进行训练。
3. 训练结果路径默认保存在数据集目录的train_result下。
4. 解决适配问题：setuptools.pypa.io/en/latest/pkg_resources.html. The pkg_resources package is slated for removal as early as 2025-11-30. Refrain from using this package or pin to Setuptools<81.
  import pkg_resources as pkg
5. 训练窗口删除Val Loss图表，增加mAp50-90图表，图表纵坐标数值显示出来。
6. 训练窗口可以最小化，但关闭要提示停止。
7. 训练日志中只显示汇总信息，中间过程信息不展示：
      Epoch    GPU_mem   box_loss   obj_loss   cls_loss  Instances       Size









